# ToDo 桌面挂件（Electron）

现在这是一个可挂在桌面的应用，不再依赖浏览器标签页。

## 功能
- 日历（月切换、待办日期高亮）
- 待办 / 已完成 / 统计 三个面板
- 任务增删改查、搜索、优先级、逾期统计
- 风格切换（浅色/深色/森林）
- 桌面挂件能力：无边框、可最小化、可关闭、可切换置顶

## 运行
```bash
npm install
npm run start
```

## 说明
- 数据仍保存在本地 `localStorage`。
- 这是桌面窗口应用，打开后可一直挂在桌面侧边使用。
